# 被污染的魔法石：弹幕与伤害重构

- 日期：2026-09-20。
- 基准：`4ae7989`；该提交及基础属性位于 main。
- 范围：最初在 `feature/polluted-magic-stone-danmaku` 独立实施；现按用户要求，已将截至 `0808750` 的全部技能和优化合入 main。下文各轮记录中的独立分支限制描述当时状态。
- 状态：主线实现与技术验证完成；可玩性、配装平衡和完整单局帧率仍需玩家实测。
- 工作区原有无关修改：文档索引、未跟踪 HANDOFF.md 和 =ro，均不属于本次提交。
- 图谱：已读取并按仓库要求更新；当前本机 graphify 只提取到 Python，不能据此证明 GDScript 调用关系。

## 目标、证据与原因

[已定设计] 用户要求 N1 实际攻击 80，直射/弧线 80%、分裂子弹 50%、激光每秒 100%；弧线弹幕需要旋转、多变、铺满画面的表现，原追踪导弹按自机弹方式重做。

[源码事实] 基准的环形弹幕只有一次 quarter_sine 齐射，追踪链持续转向；激光每次伤害来自独立 projectile_damage，并非攻击力的每秒速率。普通弹幕每帧 28 发、N1 总量 180，低帧率进一步裁剪，无法完整承载六波旋转弹型。

[源码事实] 敌弹已有对象池、集中物理更新和存档。扩展这些入口可以保留现有暂停、伤害结算及 Boss 死亡清场规则。另发现远距离分帧更新既逐帧扣寿命，又按累积步长再扣一次，实际寿命偏短。

证据文件：`enemy_boss_attacks.gd`、`enemy_boss_state.gd`、`enemy_bullet.gd`、`enemy_projectiles.gd`、`performance_guard.gd`、`runtime_spawn_budget_flow.gd`、`runtime_projectile_registry_flow.gd`。正式生成接线由 `boss_magic_stone_stats_smoke.gd` 检查。

## 方案比较与决定

| 方案 | 收益 | 代价/反对理由 | 决定 |
| --- | --- | --- | --- |
| 保持旧技能 | 无改动成本 | 不满足指定伤害和弹幕需求 | 不采用 |
| 只改伤害 | 变更少 | 单次弧线与持续追踪的行为没有改变 | 不采用 |
| 三种定时波次 + 直线自机弹 | 可组合旋转图案，玩家可通过移动引导后续发射方向 | 屏幕密度和节点数增加，需独立容量与存档测试 | 在指定分支实现 |

[已定设计的实现选择] 三种图案为原创程序几何，不复制东方游戏素材。自机弹按每轮发射时的位置瞄准、出膛后直线飞行解释；未指定的主弹、自机弹及孔雀弹幕伤害沿用旧值。完整参数见 [敌人文档](../05_敌人_Boss_关卡_难度.md)。

## 状态、系统与玩家体验

| 触发 | 状态变化 | 玩家可观察结果 |
| --- | --- | --- |
| 弧线技能冷却结束 | 轮换图案，6 波间隔 0.16 秒 | 紫粉花瓣、青橙双螺旋、绿青波纹依次出现 |
| 自机弹开始 | 7 轮三路发射，各轮重新瞄准 | 玩家移动改变后续弹路，已发出的粉色箭弹不转弯 |
| 接触激光 | 累计时间，每 0.2 秒结算；离开结清余量 | 重叠激光不翻倍，接触越久受伤越多 |
| 破盾/转阶段/死亡 | 清除待发波次和激光累计 | 演出期间没有残留计时器继续生成子弹 |
| 保存/恢复 | 保留待发序号、曲线初态、来源与分裂伤害 | 读档不重放已发波次、不让曲线突变 |

[假设] 不追踪的自机弹让持续横移、引导后小幅躲避成为可行手段。三种轨迹和颜色支持辨识；亮芯与深色轮廓维持弹体边界。彩色之外还使用轨迹差异，避免只靠颜色区分。

[待验证] 本作玩家移动、闪避、切换免伤和屏幕缩放与传统弹幕游戏不同，不能仅凭截图断言公平或平衡。需在实际地图用不同角色、常用配装与缩放各打完整三阶段，观察能否辨识空隙、引导自机弹及归因死亡。未改变角色技能、奖励、经济、输入和音频；这些设计项本次不适用。

## 工程审查与风险

- 固定 Boss 容量 4096，不使用低 FPS 裁剪；普通敌弹预算减去 Boss 专属组，防止其他怪物停火。边界测试确认满容量拒绝新增、释放后立即可用。
- 波次由 Boss 自身时间步推进，无独立 SceneTree 定时器。演出与死亡取消待发状态；曲线由出生位置、方向及累计时间解析计算，兼容分帧和存档。
- 新增字段有旧档默认值和对象池复用清理；旧激光计时不迁移成接触量。已在飞行的旧弹按原存档恢复，不改写历史伤害。
- 密集对象池取用避免整池复制；图形节点在同样式复用时继续使用。子弹数量/轨迹/画面不会因 FPS 自动缩水。
- 剩余风险：多角色、多敌群与全屏弹幕同时存在的 GPU/CPU 开销尚不能用单 Boss 微基准替代。4096 是异常保护边界，多 Boss 同时存在或未来提高发射频率时须重新核算。
- 原分裂主弹寿命先于其回收时刻，因此仍在到期时分裂；用户未要求改变时机，本次保留。

## 验收与运行证据

[运行证据] Godot 4.6.2、Windows、Vulkan Mobile / RTX 4070 SUPER。输出保存在本地 `.omx/boss-rework/`（不纳入发布资产）。

- `boss_danmaku_rework_smoke.gd`：每阶段直射伤害、三种完整波次、当前攻击缩放、子弹及 Boss JSON 存档、分裂后伤害、自机弹锁定/重新瞄准、激光 30/60/120Hz 等速率、交叠/大步长/离开/重入、对象池字段清理、Boss 容量边界、普通敌弹仍可发射、转阶段/死亡清理。
- `boss_danmaku_stress_smoke.gd`：N1 和最高压力分别连续模拟 30 秒，故意让第三阶段峰值压力保持超过实际 10 秒。N1 峰值 1823 发，最高压力峰值 3125 发；平均逻辑步约 5.20 / 9.60 ms，P95 约 7.97 / 14.09 ms，均未触及容量。不包含完整关卡渲染，不能换算成实战 FPS。
- `boss_danmaku_render_smoke.gd`：Vulkan 实际渲染三种图案，检查彩色可见像素并导出 PNG；另导出三阶段多技能叠加画面，人工检查轮廓、亮芯、箭弹和不同轨迹。截图为 `danmaku-pattern-0/1/2.png`、`danmaku-combined.png`。
- 回归覆盖基础 N1/N2 属性、护盾转换、敌人模板/快照/显示、敌弹批处理及分裂预算、玩家/敌人存档、运行时登记与伤害死亡目标。
- 仓库要求：Godot 无界面启动、`graphify update .`、`git diff --check`。

技术验收不替代可玩性验收；此分支供用户继续调技能和游玩反馈，未经请求不合入 main。

## 2026-09-20 续调：延长持续时间

[已定设计] 用户要求这些技能持续更久。本次以约增加三分之一为调整幅度，在同一技能分支实施：

| 项目 | 首版 | 本次 |
| --- | --- | --- |
| 直射、旋转弹丸留场 | 7.5 秒 | 10 秒 |
| 分裂子弹留场 | 5.7 秒 | 7.6 秒 |
| 激光持续 | 3.7 秒 | 5 秒 |
| 自机弹留场 | 3.9 秒 | 5.2 秒 |
| 旋转弹幕连续发射 | 6 波 / 0.8 秒 | 6 波 / 1 秒 |
| 自机弹连续发射 | 7 轮 / 0.54 秒 | 7 轮 / 0.72 秒 |

伤害倍率、激光 DPS、单次弹量与既有冷却参数保留。分裂主弹仍在原时机分裂；牵引和孔雀弹幕不属于本次五项技能续调。旋转弹幕在最后一波发完后才能启动下一次，防止大时间步提前重置待发状态。

[运行证据] 延长后重跑持续峰值压力，N1 峰值 2328 发、最高压力峰值 4065 发，平均逻辑步约 6.05 / 11.09 ms，P95 约 9.31 / 16.75 ms。因此固定安全容量从 4096 调为 5120，避免延长后撞上上限；此值不改变技能本身的弹量。日志为 `.omx/boss-rework/duration-stress.log`。

检查覆盖新的留场时长、尾波保护、延长后的存档连发，以及激光持续五秒累计 400 原始伤害。数值测试不代替完整关卡的实战帧率验证；留场更久会使同屏子弹增多。

## 续调：基础战斗与主题演出交替

[已定设计] 用户确认采用基础战斗、预告、主题弹幕、收尾循环，替代独立技能冷却叠加。此决定覆盖上文早期编排与常驻牵引规则；伤害及延长后的弹丸最长寿命保留，段落结束时优先清场。

基准为 `2656593`，继续在原技能分支实施。前两条血为 `12 / 1.5 / 15 / 2 秒`，第三条血为 `8 / 1.5 / 18 / 2 秒`。主题顺序为「污染绽放」「双螺旋回廊」「魔核过载」，细节和计时见 [敌人文档](../05_敌人_Boss_关卡_难度.md)。

- **方案决定**：保留旧冷却叠加无法形成段落；只让子弹活得更久会增加堆叠；采用单一段落状态调度，在每次预告和收尾清场。高输出可直接打空血条打断演出，不新增锁血或演出免伤。
- **状态所有权**：`enemy_boss_routine.gd` 负责段落、主题、已触发事件与激光锁定角度。原攻击模块只负责具体招式。Boss 停止移动时同时停止避让位移，确保预告和伤害来自同一位置。
- **表现与可读性**：复用现有 Boss HUD 状态栏显示名称和倒计时，复用聚光环显示主题色。激光用两条边界预告扫描范围，统一世界坐标中的显示长度和判定长度；第三条血强牵引先完整持续 7 秒，之后再开始波纹与激光，避免强拉和弹墙同时生效。
- **存档与边界**：保存待发波次、旋转方向、事件位及预警角度；预警中途读档不重新瞄准。清场淡出用弹丸自身时间步推进，立即失去命中和分裂能力，读档与对象池复用都覆盖。旧档没有段落状态时从基础段重启，已生成的旧弹下次清场时消失。
- **技术验收**：新增 `boss_routine_smoke.gd` 覆盖整轮循环、零时间暂停、跨段余量、基础技能门控、身体避让锁定、普通敌弹隔离、淡出存档、主题分裂、预警锁定、血条打断与旧档恢复。攻击伤害、护盾、显示、存档、对象池与 HUD 继续回归。
- **运行证据**：N1 / 最高压力各连续模拟 90 秒，覆盖三套完整主题；峰值 1273 / 2250 发，平均逻辑步 1.63 / 3.57 ms，P95 4.51 / 10.55 ms，无容量裁剪。该测试不包含完整关卡渲染，不转换为实战 FPS。日志为 `.omx/boss-rework/routine-stress.log`。
- **体验验收范围**：实际 GPU 截图检查基础、预告、三套演出、激光预警和收尾；不同角色配装下的走位难度及平衡仍待实际游玩。未更改奖励、经济、按键和音频。

## 续调：参考幽幽子扩展弹路和演出

> 历史记录：本节的蝶形、樱花外观和名称已被下一节的暗属性石质设计替代；参考的对象仅剩弹路变化与分段编排，不沿用角色意象或符卡化用。

基准 `6ba6196`，仍在技能分支；工作区已有文档索引、HANDOFF 和 UID 等无关修改，不纳入本次。图谱仅覆盖 Python，不能证明 GDScript 依赖。用户反馈花样不足，明确指定参考《东方妖妖梦》最终 Boss 幽幽子，授权继续扩展。

### 证据与设计决定

[源码事实] 原三种主体都是从中心展开的极坐标环，主题内部主要改变旋转方向与附加技能。因此只加颜色、数量或转速仍会重复同一种空间结构。

[参考证据] 直接读取 THBWiki 的「东方妖妖梦/符卡」「完全墨染的樱花」「反魂蝶」条目，核对其《妖妖梦》截图：樱花图案中可见大小/形状不同的弹体交错；反魂蝶截图显示层叠蓝/红蝶群与扇面。网页、三张原作截图和拼图仅存本地 `.omx/boss-rework/reference/`，不打包进游戏。公开来源：

- `https://thbwiki.cc/东方妖妖梦/符卡`
- `https://thbwiki.cc/完全墨染的樱花`
- `https://thbwiki.cc/反魂蝶`

这些是静态参考，不能证明原作逐帧时序。减速悬停、六主题及其具体时间是本作重编，不宣称复制原作符卡；无原作角色、音乐或图片资源导入。

[已定设计] 保留 12/8 秒基础、1.5 秒预告、15/18 秒演出、2 秒收尾。采用六主题×三小节、共十二种弹路：变化落在发射点、扇面宽度、速度层、运动路径和弹体轮廓，而非增加固定每组弹数。具体编排见敌人文档。三条血以主题 0/3/5 起步，新血条能立即见到不同图案；旧的螺旋、激光、分裂和牵引仍在轮换中。

### 状态、玩家体验与影响范围

- 新 `enemy_boss_danmaku.gd` 只生成一波图案；原攻击模块继续持有六波计时，段落仍由 `enemy_boss_routine.gd` 持有，避免新增孤立计时器。主题编号 0～2 保留，新编号 3～5；图案扩展至 0～11。
- 新 S 形与悬停再散开均由出生状态与经过时间解析计算。悬停位置不变、方向可见地旋转，重新出射有 0.65 秒加速过程；参数随存档保存，回收为普通弹时归零，难度弹速加成同时应用于再出射速度。
- HUD 复用已有状态行显示“小节名”，预告颜色及花瓣/蝶翼轮廓对应新增主题。花瓣、蝶形、米粒保留原描边、亮芯、柔光和碰撞半径；玩家辨识可以依赖形状和轨迹，非仅颜色。
- [假设] 扇面缝隙、交织弹路和悬停再散开会要求不同的走位判断，使每次演出更易分辨。碰撞、清场、伤害倍率与可打断性保持原规则；不同构筑的通过率和实际避弹体验仍须游玩验证。
- 收益、经济、成长、队伍操作、音频和基础 Boss 数值不适用：本次不改变这些系统。发布仍为独立技能分支，尚未合入 main。

### 验收

- 新 `boss_yuyuko_patterns_smoke.gd` 验证十二种完整六波、几何差异、80% 伤害、悬停/发射连续性、图案与曲线 JSON 恢复、对象池清理、六主题三小节实际触发及新血条开场。
- 渲染用例扩为十二种独立弹路和三套新主题的三段 GPU 截图；压力用例扩为 N1 / 最高压力各六个完整周期（180 秒），保持固定容量和全部技能弹量。
- [运行证据] 新图案用例、原战斗循环、伤害重构、护盾、基础属性、Boss/弹丸视觉、批量敌弹、分裂预算、玩家/敌人存档、运行时优化、登记与死亡目标共 14 项回归通过，日志为 `.omx/boss-rework/yuyuko-check-*.log`。Vulkan Mobile / RTX 4070 SUPER 实际渲染通过，人工核对红蓝蝶潮、花瓣悬停、激光及 HUD；独立图案截图仅解析轨迹，完整演出截图保留正常碰撞和回收，避免把测试靶吃弹误报成漏发。
- [运行证据] 六主题连续压力：N1 峰值 1258、平均逻辑步 1.588 ms、P95 4.022 ms；最高压力峰值 2213、平均 3.234 ms、P95 8.134 ms，均覆盖十二种弹路且未触及容量。日志为 `.omx/boss-rework/yuyuko-stress.log`。与前版三主题测试的场景组成不同，不据此宣称整局提帧。
- [运行证据] 最后完成 Godot 无界面启动、`graphify update .` 和 `git diff --check`。图谱仍仅覆盖本机可识别的 Python，不作为 GDScript 正确性证据。
- [待验证] 不同角色、配装与缩放下的实际通过率、走位空间和完整关卡帧率仍需游玩；单 Boss 测量和静态截图不能替代这些证据。

## 续调：回归被污染的魔法石设定

> 历史外观：以下碎块、尖晶、裂纹设计已由后续“统一原始球弹风格”替代；暗属性命名和弹路保留。

基准 `0a6c332`；用户明确要求不使用蝶形外观，不化用参考角色的名字，视觉须围绕暗属性。继续在技能分支修改；无关文档索引、HANDOFF、UID 等工作区变动保持原状。图谱仍仅可用于本机 Python 索引。

[源码事实] 上一版已经有十二种弹路，但外观及名称直接承接了参考角色的意象，与本作魔法石设定冲突。仅改名称不足以解决该问题；把颜色全部压黑又会降低敌弹辨识度。

[已定设计] 保留六主题、三小节及伤害/时长/弹数，以石质形状和能量裂纹建立自身身份。六主题与十二小节全部按现行敌人文档命名。单体弹丸替换为不对称黑曜碎块、尖晶、暗矛和八棱暗能球；暗色填充搭配紫/冷蓝亮边、能量裂纹和柔光。原整体蝶翼轨迹也改为三层错位破碎冲击环，其波数、伤害、寿命、碰撞半径和所属小节不变。聚能轮廓改为破碎锯齿环，自机弹使用同一暗矛外观。

- **状态与兼容**：外观标识在绘制/读档时迁移；旧蝶形、花瓣、米粒及彩色圆珠/箭弹映射为新石质外观。只更换形状和色彩，不改写伤害、运动模式、已飞行时间、碰撞和寿命。旧字段 `danmaku_bloom` 作为轨迹存档键保留，不作为显示名称。
- **辨识与性能**：保留亮边、裂纹和四层绘制，形状缓存按样式复用；没有增加特效节点或减少弹数。几何差异仍区分碎块和暗矛，色彩不作为唯一提示。
- **上下游**：HUD 从主题编号重新取名，旧档不会显示旧名称；普通敌弹和地瑰灵的玫瑰弹属于其他单位，不随最终 Boss 改动。基础数值、玩家操作、奖励、经济、音频与成长不适用，本次不改变这些系统。
- **验证计划**：复用改名后的 `boss_magic_stone_patterns_smoke.gd` 检查全部图案及分段流程；已有敌弹视觉测试增加旧样式 JSON 迁移、伤害/轨迹保持及普通弹池复用检查；用 Vulkan 实际截图检查暗场辨识度和新 HUD 名称。
- [运行证据] 上述图案、循环、伤害、弹体视觉及旧档迁移、敌人存档显示、批量弹丸、护盾、Boss 属性共 8 项检查通过，日志为 `.omx/boss-rework/darkstone-*.log`。Vulkan Mobile / RTX 4070 SUPER 渲染检查通过，人工确认破碎冲击环、暗色碎块的亮边和裂纹，以及现行 HUD 名称；截图 `darkstone-pattern-*.png` 与 `darkstone-theme-*-section-*.png`。实战平衡不由静态截图证明。

## 续调：统一原始球弹风格

基准 `2dd5481`。用户要求子弹稍亮、尽量采用紫色或黑色球，并与最初球弹和魔法石本体一致。本次只调整视觉，全部弹路与伤害、数量、时序和碰撞参数保持原值。

[源码事实] 原 `boss_dark_orb` 使用半径 8、20 边近似圆；上一版尖角碎块与细亮边虽然有暗属性配色，但与原球弹轮廓不同。采用原圆形轮廓替代碎块、细矛和裂纹，紫色球使用暗轮廓、小圆形亮芯，紫黑球使用紫色亮边；柔光透明度从 0.16 微调至 0.20，原深紫基础球弹与白芯分裂弹的主体也略提亮。配色限制在紫色范围，读档兼容两轮旧样式。

旧档只迁移外观，普通敌弹继续使用自身样式；复用原有四层视觉节点与缓存圆形几何。HUD、奖励、经济、操作、音频和成长不涉及本次调整。沿用已有的视觉迁移、图案、伤害、循环、存档与 GPU 渲染检查，重点人工检查球体与 Boss 的画面协调性。

[运行证据] 弹体视觉/九种旧样式迁移、伤害、十二图案、分段流程及敌人存档显示共 5 项检查通过；Vulkan Mobile / RTX 4070 SUPER 完整渲染检查通过，人工核对紫色实心球、紫黑球、圆形亮芯与 Boss 本体的配色。日志为 `.omx/boss-rework/orb-*.log`，截图为 `orb-pattern-*.png` 与 `orb-theme-*-section-*.png`。本次没有改写攻击逻辑，不据此宣称实战难度或帧率改变。

## 续调：完整收尾、阶段难度与角色基础提升

基准 `62b0888`，继续在 `feature/polluted-magic-stone-danmaku`。用户指定演出结束不能吞掉刚发出的弹丸，平息延长 2 秒并降低 50 护甲，旧 Boss 弹丸也使用新球体，技能按护盾/血条逐步解锁；四名可操控角色各增加 10 移速、5 攻击、50 最大生命。

[源码事实] 旧调度在演出计时结束时调用 `stop_attacks()`，取消六波及自机连发，并对全部 Boss 子弹开始 0.45 秒清场淡出。因此即使最后一组刚开始，也会马上消失。`boss_phase` 只表示三条血，护盾存在与否须另行判断。

[已定设计]

- 新增 `finishing` 收尾状态：发射段计时结束后不启动新轮次，补完当前六波、自机连发、激光及蓄力齐射；持续检查原有弹丸登记，直到包括延迟分裂子弹在内的全部弹丸自然消失。收尾不占用平息时间，HUD 显示“弹幕·主题·收尾”，不显示无意义的 0 秒。破盾、血条击破、死亡仍可即时打断。
- 平息为完整 4 秒，伤害结算读取当前状态附加 −50 护甲，不改写基础护甲或减伤率。基础护甲 10→−40，状态结束自动恢复；存档无需额外保存一份可叠加的护甲扣值。收尾和当前待发状态按原运行档保存。
- 护盾完整仅基础环弹；破盾第一条血只循环污染迸发；第二条血循环污染迸发、暗流回旋、裂隙脉冲，并解锁分裂弹；第三条血开放六主题，加入凝滞变向和引力/激光/裂晶齐射组合。各条血开场主题为 0/1/5。原弹数、单发伤害、碰撞、每组六波数量及轨迹保留。
- 旧基础环弹、分裂主弹/子弹、转向齐射与存档旧三角弹都通过现有球体绘制路径迁移；紫色/紫黑球共用圆形缓存与四层节点，普通敌弹仍走自身样式。
- 角色模板及实际生效数值：剑士移速/攻击/生命 150/25/200，枪手 160/20/170，法师 140/30/170，机械师 140/23/170。读档采用当前基础模板，保留单独保存的祝福、装备、升级与当前生命；重复载入不累加。

[运行证据]

- 14 项逻辑/界面回归通过：原循环、十二弹路、伤害、Boss 弹体迁移、玩家存档、基础防御、机械师队伍、角色面板、护盾阶段、Boss 属性、敌人存档显示、批量敌弹，加上本次新增的完整收尾及四角色平衡测试。日志为 `.omx/boss-rework/<测试名>.log`。
- 完整收尾测试强制在到时前启动六波、分裂主弹、激光和自机连发，核对末尾剩余五波与两轮自机弹精确发完，延迟子弹独立存活，并验证途中 JSON 恢复。循环测试核对实际 −40 护甲下的受伤量、完整 4 秒、读档不叠加、结束恢复及普通怪物子弹保留。
- 四角色测试从真实队伍配置启动，分别核对实际移速、攻击和满血；注入旧基础模板与受伤血量，连续两次存取，验证属性迁移和焰阳加成保留。
- Vulkan Mobile / RTX 4070 SUPER 渲染检查通过，人工核对基础环弹、收尾残留弹幕与完整 4 秒平息画面。截图 `routine-basic.png`、`routine-finishing.png`、`routine-recovery.png`；日志 `render-current.log`，错误日志为空。
- N1/高压力各模拟 270 秒，覆盖六主题与十二弹路，峰值分别 1258/2213，均低于 Boss 固定容量 5120；完整结束并通过清理检查。压力日志 `boss_danmaku_stress_smoke.log`。本轮并行执行了 GPU 验证，不以其耗时与历史单独测试比较帧率。
- 完成 Godot 无界面启动及 `graphify update .`；本机图谱仍只覆盖 Python，不能作为 GDScript 验证依据。具体走位难度与各配装通关体验尚需实际游玩反馈。

## 续调：收尾引力球与瘫痪提示

基准 `8a2d1c7`，继续保存在技能分支。工作区已有无关的文档索引改动、交接文件和 UID 文件，本次不纳入。现有图谱只覆盖 Python，以下依据直接读取的 Boss 调度、攻击、存档、HUD 与对应测试。

[源码事实] `_update_finishing()` 只在有待发自机弹或剩余牵引时更新引力球；两者结束后，仍存在的球体被冻结在最后位置。原 `recovery` 持续 4 秒，各段落均返回 HUD 状态。

[已定设计] 用户要求收尾时引力球继续运动，原平息增加 5 秒并改名为“瘫痪”，仅此期间显示状态。保留原球体速度和已有攻击的自然收尾；仅更新已有球体，不在其他主题额外生成，不启动新连发。瘫痪为完整 9 秒，沿用 −50 护甲、原碰撞伤害与存档 `recovery` 已过时间。HUD 在其余阶段隐藏状态文字及倒计时，场景聚光、橙色激光预警、血条和护盾保留。

[运行证据] 收尾、循环、十二弹路、护盾、死亡清理及敌人存档显示共 6 项回归通过。核对剩余波次与自机弹准确发完、之后每帧球体继续绕行、无球体主题不额外生成；完整 9 秒及中途存读档、最后一刻仍减甲、结束自动恢复均通过。日志位于 `.omx/boss-paralysis/`。

[运行证据] OpenGL 实际渲染回归通过：瘫痪开始显示 9.0 秒，额外窗口与回到基础段的隐藏均通过真实 HUD 断言；其余主题、预告、激光预警和收尾隐藏状态。人工核对 `routine-recovery.png` 与 `routine-finishing.png`，截图位于 `.omx/boss-rework/`，本次日志为 `.omx/boss-paralysis/render-opengl.*`。

[运行证据] 为覆盖每轮额外 5 秒瘫痪，压力检查延长到两个压力档各 300 秒；六主题、十二弹路、容量与死亡清理均通过，峰值活动弹丸分别为 1258 / 2213。Godot 无界面启动、`graphify update .` 和差异格式检查通过。不以本次数值调整宣称性能或实战平衡改善。
